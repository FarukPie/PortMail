import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  subsets: ["latin"],
  variable: "--font-sans",
});

export const metadata: Metadata = {
  title: "PortMail | Maritime Logistics Scheduler",
  description: "Schedule automated emails with Excel attachments for maritime logistics operations. Professional email scheduling for ships and vessels.",
  keywords: ["maritime", "logistics", "email scheduler", "shipping", "vessel communication"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={`${inter.variable} font-sans antialiased`}>
        {children}
      </body>
    </html>
  );
}
